spring.application.name=back-end

# CONFIGURACIÓN DE MYSQL
# Si se ejecuta en Docker, DB_HOST será 'db'. Si se ejecuta en local, será 'localhost'.
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/cms?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=ContrasenaSuperLargaYCompleja_2026!
spring.jpa.hibernate.ddl-auto=update

# Flyway Configuration
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# CONFIGURACIÓN DE MONGODB
# Si se ejecuta en Docker, MONGO_HOST será 'mongodb' y MONGO_PORT será '27017'
spring.data.mongodb.uri=mongodb://${MONGO_USER:admin}:${MONGO_PASSWORD:ContrasenaSuperLargaYCompleja_2026}@${MONGO_HOST:127.0.0.1}:${MONGO_PORT:27018}/cms_nosql?authSource=admin

management.endpoint.health.show-details=always
management.health.db.enabled=true

api.path=/
jwt.secret=$!@#$^%$$$%####$DDCPN0234FCFDPD8670M

spring.web.resources.static-locations=classpath:/static/

# -------------------------
# Thymeleaf Configuration
# -------------------------
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false    
spring.thymeleaf.encoding=UTF-8